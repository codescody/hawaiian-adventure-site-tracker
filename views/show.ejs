<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title><%= site.name %></title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="/css/style.css">
        <%-include('./partials/header.ejs')%>
    </head>
    <body>
        <div id="items-container">
            <div id="item-container">
            <h2 id="description-box"><%= site.name %>,
            <%= site.island %>
            <%= site.description %></h2>
            <img id="image-box" src=<%= site.image %>>
            <img id="map-box" src=<%= site.map %>>
            <form id="delete-box" action="/sites/<%= site.id %>?_method=DELETE" method="POST">
                <input type="submit" value="DELETE" />
            </form>
            <a id="edit-box" href="/sites/<%=site._id%>/edit">Edit</a>
            </div>
        </div>
        <script src="" async defer></script>
    </body>
</html>